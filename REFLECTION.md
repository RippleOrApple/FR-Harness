# FR-Harness 项目个人反思

我选择 FR-Harness，是因为我逐渐意识到 Coding Agent Harness 的重点并不是“让 AI 自动写代码”，而是把 AI 写代码这件事放进一个可审计、可验证、可控制的工程流程里。本项目的目标是在一个受限的 Python 工作区中，让 Agent 能读取和修改文件、运行 pytest，并在覆盖已有文件、执行测试等有风险的操作前请求人工审批。我的角色不是把每一行代码都亲手写完，而是参与需求取舍、确认设计方案、实际运行验证、反馈错误、审批关键改动，并对最终交付结果负责。AI 更多承担实现、测试补充、文档整理和反复修复的工作。这种分工让我更清楚地看到：人在 AI 辅助工程中并没有退出，只是从单纯编码者更多转向规格制定者、风险判断者和验收者。

项目中最有价值的一次调试经历是接入 DeepSeek API。起初我以为只要服务商声称兼容 OpenAI 接口，就只需要替换 API Key 和模型名即可运行；实际使用时，先遇到了 Base URL 缺少 `https://` 导致的协议错误，修正后又出现 `HTTPStatusError`。这个错误本身只说明服务端返回了非 2xx 状态码，无法直接判断是 Key 错误、余额不足、模型名不匹配，还是请求格式不符合服务商要求。通过检查 `.env`、对照服务商文档、查看审计日志和最小化请求，我才理解“接口兼容”并不等于“配置可以照搬”。这次经历直接推动了后续 `setup` 和 `doctor` 的设计：配置向导、自检命令和清晰错误提示不是附属功能，而是让项目真正可用的一部分。

在 Superpowers 工作流中，我认为最有帮助的是 `systematic-debugging`、`test-driven-development` 和 `verification-before-completion`。系统化调试让我在处理 HTTP 错误、CI 失败、Windows EXE 内部无法运行 pytest 等问题时，不再只凭感觉猜测；TDD 则把很多模糊需求压缩成可以观察的失败测试和回归测试。例如，冻结后的 EXE 中 `sys.executable` 指向程序自身，不能像普通 Python 解释器一样执行 `-m pytest`，这个问题就是通过离线 demo 和真实样例项目暴露出来的；Docker 和 Release 验证也发现 pytest 不能只放在开发依赖里，否则普通安装和容器运行时会缺少核心测试工具。完成前验证则提醒我，不能因为“本地看起来修好了”就结束，还要检查完整测试、PR、CI、Release、匿名下载和演示命令。

不过，部分流程也有接近“形式大于实质”的一面。`brainstorming` 很适合凭据存储、审批策略、Release 方案这类需要取舍的问题，因为这些决策会影响安全、易用性和作业交付；但当任务已经明确到“修复这个失败测试”或“把 `temp/` 加入 `.gitignore`”时，继续进行完整设计讨论和多轮确认，交互成本就可能超过收益。早期项目也没有严格做到每个 task 都使用全新的 subagent 和双阶段评审，后续只能在 `AGENT_LOG.md`、`SPEC_PROCESS.md` 中如实记录偏差，而不能倒签成当时已经规范执行。这个过程让我意识到，方法论可以帮助工程更有秩序，但过程记录本身不等于工程质量，真实证据和可复现验证才是关键。

TDD 在 AI 协作下总体是放大器，而不是阻碍。它确实会让前期慢一些，尤其在需求还没有完全澄清时，先写测试会显得笨重；但 AI 很容易生成看似合理、实际偏离需求的代码，TDD 迫使它面对具体行为：测试有没有被收集到、是否真的通过、失败输出是否符合预期。对 FR-Harness 来说，pytest 既是项目功能，也是约束 Agent 行为的外部裁判。只有最近一次 pytest 通过后，Agent 才能声明任务完成；模型自己的“已经修好”不能替代客观测试结果。这是我对 Agent Harness 最核心的理解之一。

subagent-driven 工作流能自主运行多久，取决于任务边界和反馈密度。我的体会是，一个 subagent 最适合处理一个可以独立验证的行为，例如空工作区创建任务、CLI 启动脚本、凭据配置、审批页面展示或 Release 构建；它不适合一次性完成整个产品。较好的 task 颗粒度应该是：目标明确、输入材料有限、有可运行的验证命令、完成后能独立合并。如果任务过大，智能体容易在局部优化中迷路；如果任务太碎，又会带来过多沟通和上下文切换。FR-Harness 后期能推进得更顺，是因为逐渐把问题拆成了 CLI 易用性、配置自检、pytest 缓存、空工作区识别、PR/CI 修复等相对独立的小块。

SPEC 和 PLAN 的质量直接影响实现质量。最典型的案例是空工作区创建 Fibonacci 程序。第一次我只输入“创建斐波那契数列计算”，Harness 默认假设目标工作区里已有项目文件，Agent 就尝试读取 `README.md`、运行 pytest、再读取 `app.py`，结果在空目录中反复失败。第二次我已经说明这是空工作区，并要求创建 `fibonacci.py` 和 `test_fibonacci.py`，但工具反馈仍然在写完源文件后提示下一步应该运行 pytest，导致 Agent 创建源文件后马上测试，却没有先创建测试文件。真正的修复不是简单要求模型“更聪明”，而是在上下文和工具反馈中加入工作区文件清单、测试文件检测、`no tests ran` 后必须创建 pytest 测试文件等明确规则。这个例子让我看到，很多 Agent 失败不是模型完全不会，而是 Harness 给出的状态、约束和下一步信号不够准确。

我最有效的 prompt/context 策略不是把所有聊天记录和项目文件都塞给模型，而是提供当前目标、相关 SPEC/PLAN、最近失败日志、验证命令和已经确认的约束。比如排查 CI 时，失败 job、关键日志和本地复现命令比完整项目历史更有用；处理空工作区时，明确告诉 Agent “当前没有文件，必须先创建源文件和测试文件”，比笼统说“把项目修好”可靠得多。这个项目也让我理解了记忆和上下文的边界：记忆应该服务于当前决策，而不是把所有历史都重新注入。

安全和分发要求也迫使我重新思考“能跑”和“能交付”的区别。API Key 不应该进入源码、日志、SQLite、审计事件或 Git 历史，所以项目最终倾向于使用 system keyring 和 Windows Credential Manager，`.env` 只保存 Base URL、模型名等非敏感配置。pytest 会执行目标项目代码，因此运行测试需要用户授权；覆盖已有文件也必须显示 diff 并逐次审批。分发方面，“在我的电脑上可以运行”和“别人可以下载并验证”完全不同。Release 需要清晰 README、可下载 ZIP、SHA256 校验、匿名下载验证、离线 demo、CI 通过和敏感信息扫描。尤其是助教允许 CLI-only Release 后，我才真正把交付重点从 WebUI 转向了 Windows x64 EXE、中文菜单、启动脚本和 CLI 自检体验。

如果重做一次，我会更早把任务拆成小的可验证单元，并从第一天维护清晰的 SPEC、PLAN 和验收清单；我会保留 TDD、系统化调试和完成前验证，但减少对明确小修复的过度 ceremony。我对 Superpowers 方法论的批判是：它假设用户有时间多轮确认，假设任务能拆成边界清楚、可测试的小步，也假设过程记录能持续保持真实。在 FR-Harness 中这些假设只部分成立：工程任务确实可以被测试约束，但模型接入、探索性调试、Windows 环境差异和交付压力经常比流程更混乱。最终我认识到，AI 能显著放大实现速度，但真正决定质量的仍然是清楚的规格、可运行的验证、诚实的过程记录，以及人对风险和交付边界的判断。
